## Agent skills

### Issue tracker

GitHub Issues is the canonical work queue; issue snapshots are synchronized one-way to the Feishu OKF knowledge base. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five default canonical triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Use a single-context local layout, with the Feishu OKF wiki as the external authoritative knowledge source. See `docs/agents/domain.md`.
